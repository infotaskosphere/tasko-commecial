import React, { useEffect, useMemo, useState } from "react";
import { Activity, CheckCircle2, Copy, KeyRound, Package, Plus, Search, ShieldCheck, Users, XCircle } from "lucide-react";
import { toast } from "sonner";
import { createLicense, getLicenseState, updateLicenseStatus } from "@/lib/licenseApi";

const MODULE_LABELS = { TASKS: "Task Management", INVOICING: "Invoicing", ACCOUNTING: "Accounting", HRMS: "HRMS" };
const initialForm = { company_name: "", contact_name: "", email: "", phone: "", package_id: "essential", validity_days: 365, max_users: 10, max_installations: 1 };
const formatDate = (value) => value ? new Date(value).toLocaleDateString("en-IN") : "Lifetime";

function Stat({ icon: Icon, label, value }) { return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="rounded-xl bg-slate-100 p-2.5 w-fit"><Icon size={18} /></div><div className="mt-4 text-3xl font-bold text-slate-950">{value}</div><div className="mt-1 text-sm font-medium text-slate-600">{label}</div></div>; }

export default function MasterConsole() {
  const [state, setState] = useState({ packages: [], licenses: [], customers: [] });
  const [loading, setLoading] = useState(true);
  const [query, setQuery] = useState("");
  const [showCreate, setShowCreate] = useState(false);
  const [form, setForm] = useState(initialForm);
  const [created, setCreated] = useState(null);

  const refresh = async () => {
    try { setState(await getLicenseState()); }
    catch (error) { toast.error(error?.response?.data?.detail || "Unable to connect to licensing server."); }
    finally { setLoading(false); }
  };
  useEffect(() => { refresh(); }, []);

  const stats = useMemo(() => ({ total: state.licenses.length, active: state.licenses.filter((l) => l.status === "active" && (!l.expires_at || new Date(l.expires_at) >= new Date())).length, expired: state.licenses.filter((l) => l.expires_at && new Date(l.expires_at) < new Date()).length, customers: new Set(state.licenses.map((l) => l.customer_id)).size }), [state]);
  const filtered = useMemo(() => { const q = query.trim().toLowerCase(); if (!q) return state.licenses; return state.licenses.filter((l) => [l.license_key, l.customer_name, l.package_name, l.status].some((v) => String(v || "").toLowerCase().includes(q))); }, [query, state.licenses]);
  const packageChange = (id) => { const pkg = state.packages.find((p) => p.id === id); setForm((f) => ({ ...f, package_id: id, max_users: pkg?.max_users || f.max_users, max_installations: pkg?.max_installations || f.max_installations })); };
  const submit = async (event) => { event.preventDefault(); try { const license = await createLicense(form); setCreated(license); setShowCreate(false); setForm(initialForm); await refresh(); toast.success("License generated on the server."); } catch (error) { toast.error(error?.response?.data?.detail || "Unable to generate license."); } };
  const copy = async (key) => { await navigator.clipboard.writeText(key); toast.success("License number copied."); };
  const changeStatus = async (license, status) => { try { await updateLicenseStatus(license.id, status); await refresh(); toast.success(`License ${status}.`); } catch (error) { toast.error(error?.response?.data?.detail || "Unable to update license."); } };

  return <div className="min-h-screen bg-slate-50 p-4 md:p-7"><div className="mx-auto max-w-[1500px]">
    <div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.18em] text-slate-400"><ShieldCheck size={15} /> Commercial Control Plane</div><h1 className="mt-1 text-3xl font-bold text-slate-950">Taskosphere Master Console</h1><p className="mt-1 text-sm text-slate-500">Server-side commercial license management.</p></div><button onClick={() => setShowCreate(true)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white"><Plus size={17} /> Generate License</button></div>
    <div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={KeyRound} label="Total Licenses" value={stats.total} /><Stat icon={CheckCircle2} label="Active Licenses" value={stats.active} /><Stat icon={XCircle} label="Expired Licenses" value={stats.expired} /><Stat icon={Users} label="Customers" value={stats.customers} /></div>
    <div className="mt-7 grid gap-5 lg:grid-cols-3"><section className="lg:col-span-2 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-bold text-slate-900">Commercial Packages</h2><p className="mt-1 text-sm text-slate-500">Entitlements are stored server-side.</p></div><Package size={20} className="text-slate-400" /></div><div className="mt-5 grid gap-4 md:grid-cols-3">{state.packages.map((pkg) => <div key={pkg.id} className="rounded-2xl border border-slate-200 p-4"><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">{pkg.code}</p><h3 className="mt-1 font-bold">{pkg.name}</h3><p className="mt-1 text-xs text-slate-500">{pkg.description}</p><div className="mt-3 flex flex-wrap gap-1.5">{pkg.modules.map((m) => <span key={m} className="rounded-full bg-slate-100 px-2 py-1 text-[11px] font-medium">{MODULE_LABELS[m] || m}</span>)}</div><p className="mt-3 text-xs text-slate-400">{pkg.max_users} users · {pkg.max_installations} installations</p></div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-slate-900">Recent Activity</h2><div className="mt-5 space-y-4">{state.licenses.slice(0, 6).map((l) => <div key={l.id} className="flex items-center gap-3"><div className="rounded-lg bg-slate-100 p-2"><Activity size={15} /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{l.customer_name}</p><p className="text-xs text-slate-400">{l.package_name}</p></div><span className="text-xs text-slate-400">{formatDate(l.issued_at)}</span></div>)}{!state.licenses.length && <p className="text-sm text-slate-400">No licenses generated yet.</p>}</div></section></div>
    <section className="mt-5 rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 p-5 md:flex-row md:items-center md:justify-between"><div><h2 className="font-bold">License Registry</h2><p className="mt-1 text-sm text-slate-500">Every license is now read from the licensing server.</p></div><div className="relative w-full md:w-80"><Search size={16} className="absolute left-3 top-3 text-slate-400" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search customer, key or package" className="w-full rounded-xl border border-slate-200 py-2.5 pl-9 pr-3 text-sm" /></div></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-400"><tr><th className="px-5 py-3">Customer</th><th className="px-5 py-3">License</th><th className="px-5 py-3">Package</th><th className="px-5 py-3">Validity</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{filtered.map((l) => <tr key={l.id}><td className="px-5 py-4"><div className="font-semibold">{l.customer_name}</div><div className="text-xs text-slate-400">{l.max_users} users · {l.max_installations} installations</div></td><td className="px-5 py-4"><button onClick={() => copy(l.license_key)} className="inline-flex items-center gap-2 rounded-lg bg-slate-100 px-2.5 py-1.5 font-mono text-xs font-semibold">{l.license_key}<Copy size={13} /></button></td><td className="px-5 py-4">{l.package_name}</td><td className="px-5 py-4 text-slate-500">{formatDate(l.expires_at)}</td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold">{l.status}</span></td><td className="px-5 py-4"><select value="" onChange={(e) => e.target.value && changeStatus(l, e.target.value)} className="rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-xs"><option value="">Action</option><option value="active">Activate</option><option value="suspended">Suspend</option><option value="revoked">Revoke</option></select></td></tr>)}{!filtered.length && <tr><td colSpan="6" className="px-5 py-12 text-center text-slate-400">{loading ? "Loading licensing server…" : "No licenses found."}</td></tr>}</tbody></table></div></section>
    {created && <div className="fixed inset-x-4 bottom-5 z-50 mx-auto max-w-2xl rounded-2xl border border-emerald-200 bg-white p-5 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-emerald-600">License Generated</p><h3 className="mt-1 font-bold">{created.customer_name}</h3><p className="text-sm text-slate-500">{created.package_name}</p></div><button onClick={() => setCreated(null)} className="text-slate-400">×</button></div><div className="mt-4 flex items-center gap-2 rounded-xl bg-slate-950 p-3"><code className="flex-1 break-all text-sm font-bold tracking-wider text-white">{created.license_key}</code><button onClick={() => copy(created.license_key)} className="rounded-lg bg-white/10 p-2 text-white"><Copy size={16} /></button></div></div>}
    {showCreate && <div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-950/40 p-4"><form onSubmit={submit} className="w-full max-w-2xl rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">Generate License</h2><p className="mt-1 text-sm text-slate-500">Create a server-side commercial entitlement.</p></div><button type="button" onClick={() => setShowCreate(false)} className="text-slate-400">×</button></div><div className="mt-6 grid gap-4 md:grid-cols-2"><label className="text-sm font-medium">Company Name<input required value={form.company_name} onChange={(e) => setForm({ ...form, company_name: e.target.value })} className="mt-1.5 w-full rounded-xl border px-3 py-2.5" /></label><label className="text-sm font-medium">Contact Person<input value={form.contact_name} onChange={(e) => setForm({ ...form, contact_name: e.target.value })} className="mt-1.5 w-full rounded-xl border px-3 py-2.5" /></label><label className="text-sm font-medium">Email<input type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className="mt-1.5 w-full rounded-xl border px-3 py-2.5" /></label><label className="text-sm font-medium">Phone<input value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} className="mt-1.5 w-full rounded-xl border px-3 py-2.5" /></label><label className="text-sm font-medium">Package<select value={form.package_id} onChange={(e) => packageChange(e.target.value)} className="mt-1.5 w-full rounded-xl border px-3 py-2.5"><option value="essential">Essential — Tasks + Invoicing</option><option value="professional">Professional — Tasks + Invoicing + HRMS</option><option value="enterprise">Enterprise — Tasks + Invoicing + Accounting + HRMS</option></select></label><label className="text-sm font-medium">Validity<select value={form.validity_days} onChange={(e) => setForm({ ...form, validity_days: Number(e.target.value) })} className="mt-1.5 w-full rounded-xl border px-3 py-2.5"><option value="365">1 Year</option><option value="730">2 Years</option><option value="1095">3 Years</option><option value="0">Lifetime</option></select></label><label className="text-sm font-medium">Maximum Users<input type="number" min="1" value={form.max_users} onChange={(e) => setForm({ ...form, max_users: Number(e.target.value) })} className="mt-1.5 w-full rounded-xl border px-3 py-2.5" /></label><label className="text-sm font-medium">Maximum Installations<input type="number" min="1" value={form.max_installations} onChange={(e) => setForm({ ...form, max_installations: Number(e.target.value) })} className="mt-1.5 w-full rounded-xl border px-3 py-2.5" /></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setShowCreate(false)} className="rounded-xl border px-4 py-2.5 text-sm font-semibold">Cancel</button><button type="submit" className="rounded-xl bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white">Generate License</button></div></form></div>}
  </div></div>;
}
